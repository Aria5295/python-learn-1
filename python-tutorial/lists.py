friends = ["Kevin", 2, "Jim", "Oscar", "Toby"]

print(friends)

friends[1] = "Mike"

print(friends[1])

ages = [15, 16, 14]

print(ages[1:])

#list functions
'''
    *print([listname]) - prints the list
    *[listname].extend(secondlist) - appends second list onto first list
    *[listname].append([element]) - adds the element parameter onto the list
    *[listname].insert([index], [element]) - adds the element at the specified index into the list
    *[listname].remove([element]) - removes the specified element
    *[listname].clear - clears out the list so it's empty
    *[listname].pop - pops out the last element
    *[listname].index([element]) - tells what index the element is in
    *[listname].count([element]) - tells how many times the element is in the list
    *[listname].sort - sorts alphanumerically
    *[listname].reverse - the list backwards
    *[var name] = [listname].copy - copies the list
'''
